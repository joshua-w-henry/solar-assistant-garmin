(:background)
module SolarConfig {

    // Public HTTPS endpoint implementing the JSON contract in docs/API.md.
    const API_URL = "https://solar.example.com/v1/summary";

    // Use a dedicated, read-only bridge token. Never commit the real value.
    const API_TOKEN = "ENTER_YOUR_BRIDGE_TOKEN_HERE";
}
